% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event_rate_limit.R
\name{event_debounce}
\alias{event_debounce}
\title{Debounce an event callback}
\usage{
event_debounce(fn, ms, coalesce = TRUE)
}
\arguments{
\item{fn}{An event handler function.}

\item{ms}{Quiet period in milliseconds before firing.}

\item{coalesce}{If \code{TRUE} (default), also gate on server idle so
events never queue faster than the server can process them.}
}
\value{
A wrapped handler.
}
\description{
Waits until the user pauses for \code{ms} milliseconds before firing.
With \code{coalesce = TRUE} (the default), also waits for the server to
finish processing, so events never queue up.
}
