% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/event.R
\name{event_throttle}
\alias{event_throttle}
\title{Throttle an event callback}
\usage{
event_throttle(fn, ms, leading = TRUE, coalesce = TRUE)
}
\arguments{
\item{fn}{An event handler function.}

\item{ms}{Minimum interval in milliseconds between events.}

\item{leading}{If \code{TRUE} (default), fire immediately on the first
event. If \code{FALSE}, wait for the timer before firing.}

\item{coalesce}{If \code{TRUE} (default), also gate on server idle so
events never queue faster than the server can process them.}
}
\value{
A wrapped handler.
}
\description{
Fires at most every \code{ms} milliseconds while the event is active.
With \code{coalesce = TRUE} (the default), also waits for the server to
finish processing before sending the next event, so the effective rate is
\code{max(ms, server_processing_time)}.
}
